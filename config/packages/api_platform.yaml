api_platform:
    title: 'FIL ROUGE SYMFONY BY MOUCTAR DIALLO'
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
    # collection:
    #     pagination:
    #         items_per_page: 5
